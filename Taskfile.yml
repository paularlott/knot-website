version: "3"

tasks:
  default:
    desc: "Show available tasks"
    cmd: task --list

  build:
    desc: "Build the site (diagrams are inline Mermaid — no generation step needed)"
    cmd: "hugo ${HUGO_ARGS:-}"
