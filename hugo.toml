baseURL = 'https://getknot.dev/'
languageCode = 'en-US'
title = 'knot'

# OKF frontmatter uses `tags` as dormant metadata (consumed by scripts/okf.py and
# the OKF bundles), not as browseable site taxonomies. Suppress the auto-generated
# /tags/ listing pages so tags stay metadata-only.
disableKinds = ['taxonomy', 'term']

[outputs]
  home = ['HTML', 'JSON']

[params]
  githubURL = "https://github.com/paularlott/knot"
  copyright = "Paul Arlott"
  defaultTheme = "light"  # Options: "light" or "dark"
  includeSections = ["docs", "reference"]

  [params.navigation]
    [[params.navigation.header]]
      name = 'Home'
      url = '/'
    [[params.navigation.header]]
      name = 'Docs'
      url = '/docs'
    [[params.navigation.header]]
      name = 'Reference'
      url = '/reference'
    [[params.navigation.header]]
      name = 'Core vs Pro'
      url = '/core-vs-pro'
    [[params.navigation.header]]
      name = 'Changelog'
      url = '/changelog'

    # Static navigation links
    # [[params.navigation.static]]
    #   name = 'Test Page'
    #   url = '/testing'
    # [[params.navigation.static]]
    #   name = 'Changelog'
    #   url = '/changelog'

[build]
  [build.buildStats]
    enable = true
  [[build.cachebusters]]
    source = 'assets/notwatching/hugo_stats\.json'
    target = 'css'
  [[build.cachebusters]]
    source = '(postcss|tailwind)\.config\.js'
    target = 'css'

[module]
  [module.hugoVersion]
    extended = true
    min = '0.150.0'

  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true

  [markup.highlight]
    #style = 'catppuccin-frappe'
    noClasses = false
    lineNoStart = 1
    lineNos = false
    lineNumbersInTable = false
    tabWidth = 2

  [[module.mounts]]
    source = 'assets'
    target = 'assets'
  [[module.mounts]]
    source = "assets/scss"
    target = "assets/scss"
  [[module.mounts]]
    disableWatch = true
    source = 'hugo_stats.json'
    target = 'assets/notwatching/hugo_stats.json'
